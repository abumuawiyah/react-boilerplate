import React from 'react';
import { shallow } from 'enzyme';
import {{ properCase name }}Module from '../index';


const render = () =>
	shallow(<{{ properCase name }}Module />);


describe('<{{ properCase name }}Module />', () => {
	it('exists', () => {
		expect(render().exists()).toBe(true);
    })
})